"Test creating numpy array":
  - do:
      python.execute:
        body:
          script:
              source: "import numpy as np\na = np.array([[1, 2, 3],[4, 5, 6]])\na.shape"

  - match: { "result": '(2, 3)' }
