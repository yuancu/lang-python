"Test creating numpy array":
  - do:
      python.execute:
        body:
          script:
              source: |
                import numpy as np
                a = np.array([[1, 2, 3], [4, 5, 6]])
                a.shape

  - match: { "result": '(2, 3)' }
